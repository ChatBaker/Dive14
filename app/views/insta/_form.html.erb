<%= form_with(model: instum, local: true) do |form| %>
  <% if instum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(instum.errors.count, "error") %> prohibited this instum from being saved:</h2>

      <ul>
      <% instum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content, id: :instum_content %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
